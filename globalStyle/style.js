import {StyleSheet} from 'react-native'; 

export const globalStyles = StyleSheet.create({
    container:{
        flex:1
    },
    backgroundImage: {
        flex:1,
        resizeMode:'contain',
      },
})
